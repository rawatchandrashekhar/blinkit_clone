const refresh_tokens = async () => {
  try {
    // will call refresh token API and pass last one refresh token as a parameter
  } catch (error) {}
};

const refetchUser = async (userDetails: any) => {
  try {
    // will call refetch user API and pass user details object that will be required
  } catch (error) {}
};
